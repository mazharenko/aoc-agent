The Agent is designed to assist with the Advent of Code solving process. It will hardly perform any work if all the available puzzles are already solved and no new solutions are implemented.

It currently has command-line arguments for running a specific day or part, or benchmarking all the solutions.

So, it's supposed to be used the following way:

1. Save the `session` cookie value from https://adventofcode.com/ to the `SESSION.COOKIE` file. If the file does not exist, the agent can 